export interface User {
    uid: string;
    email: string;
    displayName: string;
    photoURL: string;
    emailVerified: boolean;
 }

 export interface BasicFirebaseData {
      title: string;
      message: string;
      imageUrl: string;
      url: string;
 }